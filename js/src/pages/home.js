/** @jsx React.DOM */

var React = require('react');

var Home = React.createClass({
    render: function() {
        return (
            <p>This is the root page</p>
        );
    }
});

module.exports = Home;
